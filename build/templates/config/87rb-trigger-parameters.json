{
  "HttpServer":{
    "Port": {{TriggerHttpPort}}
  },
  "database":{
    "core":{
      "connectionString": "user={{CoreDbTriggerUser}} dbname={{CoreDb}} host={{CoreDbHost}} password={{CoreDbPassword}}"
    }
  }
}