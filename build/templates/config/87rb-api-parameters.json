{
  "facilities":{
    "httpServer":{
      "listenPort": {{HttpPort}}
    }
  },
  "database":{
    "core":{
      "connectionString": "user={{CoreDbUser}} dbname={{CoreDb}} host={{CoreDbHost}} password={{CoreDbPassword}}"
    }
  }
}