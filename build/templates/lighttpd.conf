server.document-root = "/var/www/87rb-ui"

server.port = {{PublicUiPort}}

mimetype.assign = (
  ".tar.gz" => "application/x-tgz",
  ".gz" => "application/x-gzip",
  "" => "text/plain"
)

server.username = "87rb"
server.groupname = "87rb"